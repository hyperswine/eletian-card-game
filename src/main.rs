fn main() {
    println!("Hello, Eletia!");

    // setup config

    // only load the cards that the player has from assets/user_state.yml
    // and any that is on display

    // when duelling enemy
    // load the enemy cards too, from assets/cards
}

use terraformer3d::Camera3D;
use terraformer3d::game::entity::Entity;
use terraformer3d::game::game_controller;
